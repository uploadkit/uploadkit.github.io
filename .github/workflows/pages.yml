name: Deploy docs site

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install Pagefind
        run: npm install --no-save pagefind@1.3.0

      - name: Build search index
        run: npx pagefind --site . --output-subdir pagefind --glob "{index.html,docs/**/*.html}"

      - name: Stage site for deploy
        run: |
          mkdir -p _site
          # Copy published site assets only (exclude git metadata and workflow sources)
          shopt -s dotglob nullglob
          for item in *; do
            case "$item" in
              .git|.github|_site|node_modules) continue ;;
            esac
            cp -a "$item" _site/
          done
          # Keep .nojekyll for Pages
          touch _site/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
